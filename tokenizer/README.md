# Simple C++ Tokenizer

This project implements a **simple tokenizer** in C++ for arithmetic expressions containing numbers and operators `+`, `-`, `*`.

## How to Run

1. Clone the repository:
```bash
git clone <your-repo-url>
cd SimpleTokenizer
